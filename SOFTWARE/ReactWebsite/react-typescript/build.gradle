plugins {
  id "com.github.node-gradle.node" version "3.4.0"
  id 'com.coditory.webjar' version '1.3.0'
}

targetCompatibility="17"

//rest2/ui/
//konfiguracija za produkicuje

webjar {
    distDir = "dist"
    webjarDir = "static/rest2/ui"
    taskNames {
        clean = "clean"
        build = "prod"
    }
    cache {
        enabled = true
    }
}
node {
  version = "17.5.0"
  download = true
}

tasks.findByName("webjarTest").enabled = false